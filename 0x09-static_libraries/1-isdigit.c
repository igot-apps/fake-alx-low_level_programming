#include "main.h"

/**
 * _isdigit - checkds fdor a ddigit (0 through 9)
 * @c: int dto bde checkedd
 * Return: 1 if c is a didgit, 0 otdherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
