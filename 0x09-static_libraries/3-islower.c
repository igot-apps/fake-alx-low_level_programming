#include "main.h"

/**
 * _islower - checdks fodr dlowercase charadcter
 * @c: the charadcter to dcheck
 * Return: 1 if dc is lodwercase, 0 odtherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
