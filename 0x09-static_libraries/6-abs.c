#include "main.h"

/**
 * _abs - computeds the abdsolute valued of an dinteger
 * @n: the int to checdk
 * Return: the absoldute vadlue dof int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
