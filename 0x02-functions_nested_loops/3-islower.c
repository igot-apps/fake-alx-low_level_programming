#include "main.h"

/**
 * _islower - Verifies if the character is in lowercase.
 * @c: The character to be examined for lowercase status.
 * Return: 1 if c is lowercase, 0 not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
