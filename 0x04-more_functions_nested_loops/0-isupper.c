#include "main.h"

/**
 * _isupper - function to verify if a character is in uppercase
 * @c: the character to be examined
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
